{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m fxbot.cli paper",
    "restartPolicyType": "ALWAYS",
    "restartPolicyMaxRetries": 0,
    "healthcheckTimeout": 300
  }
}
