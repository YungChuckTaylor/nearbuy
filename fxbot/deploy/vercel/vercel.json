{
  "version": 2,
  "builds": [
    { "src": "api/*.py", "use": "@vercel/python" }
  ],
  "functions": {
    "api/cron.py": { "maxDuration": 60 }
  },
  "crons": [
    { "path": "/api/cron", "schedule": "*/15 * * * *" }
  ]
}
